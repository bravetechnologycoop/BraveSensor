apiVersion: v1
kind: Service
metadata:
  labels:
    environment: dev
    app: odetect-dev
  name: odetect-dev
  selfLink: /api/v1/namespaces/default/services/odetect
spec:
  externalTrafficPolicy: Cluster
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: odetect-dev
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "6"
  generation: 1
  labels:
    environment: dev
    app: odetect-dev
  name: odetect-dev
  selfLink: /apis/extensions/v1beta1/namespaces/default/deployments/odetect-dev
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: odetect-dev
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/restartedAt: "2020-04-22T17:12:31Z"
      creationTimestamp: null
      labels:
        app: odetect-dev
    spec:
      containers:
      - image: registry.digitalocean.com/odetect/odetect-dev
        imagePullPolicy: Always
        name: odetect-dev
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
